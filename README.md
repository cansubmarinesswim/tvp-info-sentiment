# `tvp-info` scraper and analysis - Work in progress!

Scraping `opinie` articles from `tvp.info` using scrapy and bs4

## Installation
Setup the virtual environment and install modules from `requirements.txt`
